# Install Python 
# Install VS Code

# Create Virtual Environment -

    python -m venv venv

# Install Boto3 -

    pip install boto3

# Configure AWS Credentials
- Install AWS CLI
- Authenticate using:

      aws configure

  
